# navigation-route-planner
A full-stack graph-based navigation application that helps users find routes between cities using BFS (Breadth First Search) and DFS (Depth First Search) algorithms. The system allows users to manage cities and road connections, visualize graphs interactively, and discover the shortest path between locations through an intuitive web interface.
